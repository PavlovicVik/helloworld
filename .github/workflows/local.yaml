name: hello-world
'on': push
jobs:
  docker_local_run:
    runs-on: self-hosted
    steps:
     - name: Checkout
       uses: actions/checkout@v3
     - name: Compose
       uses: isbang/compose-action@v1.4.1
       with:
         compose-file: "./helloworld/docker-compose.yaml"
     
   
